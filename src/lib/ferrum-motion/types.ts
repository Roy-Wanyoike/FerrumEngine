// ════════════════════════════════════════════════════════════════
// Re-export stub — code moved to @roycss/effects (ADR-011)
// ════════════════════════════════════════════════════════════════

export type {
  SpringController,
  SpringConfig,
  DecayController,
  DecayConfig,
  TimelineSequence,
  TimelineOptions,
  TimelineController,
  ScrollOptions,
  ScrollCallback,
  StaggerOptions,
} from '@roycss/effects/ferrum-motion/types';
