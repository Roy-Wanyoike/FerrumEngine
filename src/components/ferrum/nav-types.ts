/**
 * Nav types — re-exported from the canonical @/lib/types module.
 * Import from here for backward compatibility, or import directly
 * from @/lib/types for new code.
 */
export type { ViewId, NavProps, MegaMenuItem, MegaMenuGroup } from "@/lib/types";
